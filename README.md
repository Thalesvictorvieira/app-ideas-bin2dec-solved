# app-ideas-bin2dec-solved
A simple command-line application written in Python that converts numbers between decimal and binary formats. The program allows users to choose the conversion direction through an interactive menu and displays the result directly in the terminal.
